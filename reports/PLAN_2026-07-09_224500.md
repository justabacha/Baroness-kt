# Baroness-kt Implementation Plan: Unified Notifications

**Date:** 2026-07-09 22:45:00
**Author:** AI Agent
**Status:** Pending Approval

---

## Issue #1: Project Setup & Dependencies
### Current State
Firebase Cloud Messaging (FCM) is not integrated into the project.

### Proposed Change
Add FCM dependencies to `app/build.gradle.kts` and update `AndroidManifest.xml` with required permissions and service declarations.

### Why This Approach
Standard integration for FCM on Android.

### Files to Touch
- `app/build.gradle.kts` — Add `com.google.firebase:firebase-messaging-ktx`.
- `app/src/main/AndroidManifest.xml` — Add `POST_NOTIFICATIONS` permission (if missing), declare `FCMService`, and set `MainApplication`.

---

## Issue #2: Core Notification Infrastructure
### Current State
No centralized system for handling push or in-app notifications.

### Proposed Change
Create `NotificationData.kt`, `NotificationManager.kt`, and `NotificationViewModel.kt`.

### Why This Approach
`NotificationManager` provides a clean API for local push notifications, while `NotificationViewModel` manages the state for in-app UI.

### Files to Touch
- `[NEW] app/src/main/java/com/baroness/app/data/models/NotificationData.kt`
- `[NEW] app/src/main/java/com/baroness/app/utils/NotificationManager.kt`
- `[NEW] app/src/main/java/com/baroness/app/viewmodels/NotificationViewModel.kt`

---

## Issue #3: Push Notification Service & Global Init
### Current State
App cannot receive FCM messages or initialize notification channels globally.

### Proposed Change
Create `FCMService.kt` to handle incoming messages and `MainApplication.kt` to register notification channels.

### Why This Approach
`FirebaseMessagingService` is the entry point for push; `Application` class ensures channels exist before any notification is sent.

### Files to Touch
- `[NEW] app/src/main/java/com/baroness/app/services/FCMService.kt`
- `[NEW] app/src/main/java/com/baroness/app/MainApplication.kt`

---

## Issue #4: In-App Notification UI Component
### Current State
No UI component for displaying non-intrusive notifications inside the app.

### Proposed Change
Create `InAppNotification.kt` composable following the design system.

### Why This Approach
Provides a consistent, reusable UI for in-app alerts as per `FEATURE_notification.md`.

### Files to Touch
- `[NEW] app/src/main/java/com/baroness/app/components/notification/InAppNotification.kt`

---

## Issue #5: Feature Integration & FCM Token Management
### Current State
FCM tokens are not saved to Supabase, and realtime events don't trigger in-app notifications.

### Proposed Change
Update `ProfileManager.kt` to handle `fcm_token` and `WishlistRepository.kt` to trigger `NotificationViewModel`.

### Why This Approach
Ensures the server knows where to send push notifications and the UI reacts to foreground events.

### Files to Touch
- `app/src/main/java/com/baroness/app/modules/ProfileManager.kt` — Add `updateFcmToken`.
- `app/src/main/java/com/baroness/app/repository/WishlistRepository.kt` — Trigger in-app notification on `Insert`.

---

## Issue #6: Navigation & In-App UI Overlay
### Current State
`MainActivity` doesn't handle deep links or display the in-app notification overlay.

### Proposed Change
Update `MainActivity.kt` to collect `NotificationViewModel` state and show the `InAppNotification` component at the top level.

### Why This Approach
The top-level `Surface` in `MainActivity` is the correct place to overlay global UI components.

### Files to Touch
- `app/src/main/java/com/baroness/app/MainActivity.kt`

---

## Summary
- Total files modified: 5 (existing) + 6 (new) = 11
- Estimated complexity: Medium
- Confidence level: 9/10
